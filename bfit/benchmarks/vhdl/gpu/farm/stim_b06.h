// AUTO-GENERATED by gen_stim.py for b06 -- mirrors gen_tb.py
#define STIM_NAME "b06"
#define STIM_RST _reset
#define STIM_HAS_RST 1
SM_DEVICE static inline void stim_drive(uint32_t *lfsr, inputs_t *in) {
  uint32_t l = *lfsr, fb;
  // eql : ('bit',)
  for (int i = 0; i < 1; i++) { fb = ((l>>31)^(l>>21)^(l>>1)^l)&1u; l = (l<<1)|fb; }
  in->_eql = l & 1u;
  // cont_eql : ('bit',)
  for (int i = 0; i < 1; i++) { fb = ((l>>31)^(l>>21)^(l>>1)^l)&1u; l = (l<<1)|fb; }
  in->_cont_eql = l & 1u;
  *lfsr = l;
}
SM_DEVICE static inline uint64_t stim_fold(uint64_t r, const outputs_t *o) {
  for (int i = 1; i >= 0; i--) { r = (r<<1)|(r>>63); r ^= (o->_cc_mux >> i) & 1u; }
  for (int i = 1; i >= 0; i--) { r = (r<<1)|(r>>63); r ^= (o->_uscite >> i) & 1u; }
  r = (r<<1)|(r>>63); r ^= (o->_enable_count & 1u);
  r = (r<<1)|(r>>63); r ^= (o->_ackout & 1u);
  return r;
}
