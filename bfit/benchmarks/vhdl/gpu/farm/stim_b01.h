// AUTO-GENERATED by gen_stim.py for b01 -- mirrors gen_tb.py
#define STIM_NAME "b01"
#define STIM_RST _reset
#define STIM_HAS_RST 1
SM_DEVICE static inline void stim_reset(inputs_t *in, int active) { in->_reset = active ? 1 : 0; }
SM_DEVICE static inline void stim_drive(uint32_t *lfsr, inputs_t *in) {
  uint32_t l = *lfsr, fb;
  // line1 : ('bit',)
  for (int i = 0; i < 1; i++) { fb = ((l>>31)^(l>>21)^(l>>1)^l)&1u; l = (l<<1)|fb; }
  in->_line1 = l & 1u;
  // line2 : ('bit',)
  for (int i = 0; i < 1; i++) { fb = ((l>>31)^(l>>21)^(l>>1)^l)&1u; l = (l<<1)|fb; }
  in->_line2 = l & 1u;
  *lfsr = l;
}
SM_DEVICE static inline uint64_t stim_fold(uint64_t r, const outputs_t *o) {
  r = (r<<1)|(r>>63); r ^= (o->_outp & 1u);
  r = (r<<1)|(r>>63); r ^= (o->_overflw & 1u);
  return r;
}
