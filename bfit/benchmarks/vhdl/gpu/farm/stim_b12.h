// AUTO-GENERATED by gen_stim.py for b12 -- mirrors gen_tb.py
#define STIM_NAME "b12"
#define STIM_RST _reset
#define STIM_HAS_RST 1
SM_DEVICE static inline void stim_drive(uint32_t *lfsr, inputs_t *in) {
  uint32_t l = *lfsr, fb;
  // start : ('bit',)
  for (int i = 0; i < 1; i++) { fb = ((l>>31)^(l>>21)^(l>>1)^l)&1u; l = (l<<1)|fb; }
  in->_start = l & 1u;
  // k : ('vec', 3, 0, 4)
  for (int i = 0; i < 4; i++) { fb = ((l>>31)^(l>>21)^(l>>1)^l)&1u; l = (l<<1)|fb; }
  in->_k = (uint64_t)(l & 0xfu);
  *lfsr = l;
}
SM_DEVICE static inline uint64_t stim_fold(uint64_t r, const outputs_t *o) {
  r = (r<<1)|(r>>63); r ^= (o->_nloss & 1u);
  for (int i = 3; i >= 0; i--) { r = (r<<1)|(r>>63); r ^= (o->_nl >> i) & 1u; }
  r = (r<<1)|(r>>63); r ^= (o->_speaker & 1u);
  return r;
}
