// AUTO-GENERATED by gen_stim.py for b22 -- mirrors gen_tb.py
#define STIM_NAME "b22"
#define STIM_RST _reset
#define STIM_HAS_RST 1
SM_DEVICE static inline void stim_drive(uint32_t *lfsr, inputs_t *in) {
  uint32_t l = *lfsr, fb;
  // si : ('int', None)
  for (int i = 0; i < 1; i++) { fb = ((l>>31)^(l>>21)^(l>>1)^l)&1u; l = (l<<1)|fb; }
  in->_si = (uint64_t)(l & 0xffu);
  *lfsr = l;
}
SM_DEVICE static inline uint64_t stim_fold(uint64_t r, const outputs_t *o) {
  r = (r<<1)|(r>>63); { int32_t v = (int32_t)(uint32_t)o->_so; int32_t mm = v % 65536; if (mm < 0) mm += 65536; r ^= (uint64_t)mm; }
  r = (r<<1)|(r>>63); r ^= (o->_rd & 1u);
  r = (r<<1)|(r>>63); r ^= (o->_wr & 1u);
  return r;
}
